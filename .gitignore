# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# 构建产物
build/
dist/
*.spec

# 用户数据 / 数据库 / 备份 / 日志 / 报告（隐私，绝不入库）
data/

# 同步凭证（敏感，绝不入库）
data/sync_auth.json
data/sync_config.json

# 临时文件
_inspect_db.py
_*.py
*.tmp

# Reasonix 内部配置与目录
.reasonix/
reasonix.toml

# 打包产物
*.exe

# GitHub 开源版副本（独立仓库用，不随主仓库提交）
github_version/

# 本机自用的一键打包脚本（含固定安装路径/桌面快捷方式，不适合开源分享）
update.py

# 归档/备份目录（保留在磁盘，不入库）
_archive_*/
mobile_backup_*/

# 测试临时目录
.pytest_tmp*/
.pytest_cache/
